<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/moviesList">

    <fragment
        android:id="@+id/moviesList"
        android:name="com.scribblex.moviedb.view.MoviesListFragment"
        android:label="@string/movies_list">
        <action
            android:id="@+id/movieList_to_movieDetailScreen"
            app:destination="@id/movieDetailFragment" />
    </fragment>

    <fragment
        android:id="@+id/movieDetailFragment"
        android:name="com.scribblex.moviedb.view.MovieDetailFragment"
        android:label="@string/movie_detail" />

</navigation>